apiVersion: v1
kind: Service
metadata:
  name: mailpit
  namespace: repairshop
spec:
  type: LoadBalancer
  ports:
    - port: 1025
      targetPort: 1025
      name: smtp
    - port: 8025
      targetPort: 8025
      name: http
  selector:
    app: mailpit
